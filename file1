import g4f

prompt = 'Какая у тебя версия gpt?'

response = g4f.ChatCompletion.create(
    model="gpt-3.5-turbo",
    messages=[{"role": "user", "content": prompt}],
    stream=True,
)

for message in response:
    print(message, flush=True, end='')
